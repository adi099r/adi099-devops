apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devops-project-timer-python-app.fullname" . }}-test-connection"
  labels:
    {{- include "devops-project-timer-python-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devops-project-timer-python-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
