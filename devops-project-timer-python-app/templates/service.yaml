apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-project-timer-python-app.fullname" . }}
  labels:
    {{- include "devops-project-timer-python-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "devops-project-timer-python-app.selectorLabels" . | nindent 4 }}
